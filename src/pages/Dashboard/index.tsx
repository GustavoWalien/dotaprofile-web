import React from 'react';

import {
  Container, Title,
} from './styles';

const Dashboard: React.FC = () => {
  const test = '';

  return (
    <Container>
      <Title>Dota Profile</Title>
    </Container>
  );
};

export default Dashboard;
